export default function Pagination({page,totalPages,onChange}){if(totalPages<=1)return null;return <nav className="pagination" aria-label="Paginación"><button type="button" disabled={page===1} onClick={()=>onChange(page-1)}>Anterior</button>{Array.from({length:totalPages},(_,i)=>i+1).map(n=><button type="button" key={n} className={page===n?"active":""} onClick={()=>onChange(n)} aria-current={page===n?"page":undefined}>{n}</button>)}<button type="button" disabled={page===totalPages} onClick={()=>onChange(page+1)}>Siguiente</button></nav>;}